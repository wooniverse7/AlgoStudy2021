#include <string>
#include <vector>

using namespace std;

vector<int> solution(string s) {
    vector<int> answer;
    s.erase(s.begin() + 1);
    s.erase(s.end() - 1);

    for (int i = 0; i < s.size() - 1; i++) {
        if ()
    }

    return answer;
}